{
  "name": "rx-with-flowtype",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "npm run build && node lib/index.js",
    "fmt": "prettier --single-quote --write --semi=false \"src/**/*.js\"",
    "build": "babel src -d lib"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.4.0",
    "babel-preset-flow": "^6.23.0",
    "flow-bin": "^0.44.2",
    "prettier": "^1.1.0"
  },
  "dependencies": {
    "redux": "^3.6.0",
    "redux-observable": "^0.14.1",
    "rxjs": "^5.3.0"
  }
}
